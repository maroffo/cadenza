# ABOUTME: Keeps secrets and local-only files out of the Docker build context entirely.
# ABOUTME: Mirrors the secret entries of .gitignore; build layers and CI caches retain whatever COPY ingests.

.git/
.env
.env.*
*.local.json
service-account*.json
quality_reports/
.continue-here.md
.DS_Store
.idea/
.vscode/
bin/
cadenza
*.test
*.out
coverage.*
